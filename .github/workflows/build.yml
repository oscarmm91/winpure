name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    # Needed by the "Attach to release" step below: the default GITHUB_TOKEN is read-only,
    # which is why every tag build used to go red at that last step.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build (Debug)
        run: dotnet build WinPure.sln -c Debug

      - name: Engine tests (backup / revert)
        run: dotnet run --project tests/WinPure.EngineTests --no-build

      - name: Publish portable exe
        run: dotnet publish src/WinPure/WinPure.csproj -c Release -o publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: WinPure
          path: publish/WinPure.exe

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: publish/WinPure.exe
